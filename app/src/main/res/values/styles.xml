<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- Button Style -->
    <style name="ButtonStyle" parent="@android:style/ButtonBar">
        <item name="android:background">?android:attr/selectableItemBackgroundBorderless</item>
        <item name="android:textColor">@color/colorWhite</item>
        <item name="android:textSize">15sp</item>
        <item name="android:textAllCaps">false</item>
    </style>

    <style name="ScoreButton" parent="ButtonStyle">
        <item name="android:layout_width">60dp</item>
        <item name="android:layout_height">60dp</item>
        <item name="android:layout_margin">20dp</item>
        <item name="android:background">@drawable/background</item>
        <item name="android:contentDescription">@string/minus_btn_desc</item>
        <item name="android:scaleType">centerInside</item>
    </style>

    <!-- Text Styles -->
    <style name="customTextStyle" parent="@android:style/TextAppearance.DeviceDefault.Medium">
        <item name="android:textColor">@color/colorWhite</item>
        <item name="android:fontFamily">sans-serif</item>
    </style>

    <style name="TitleTextStyle" parent="customTextStyle">
        <item name="android:textSize">50sp</item>
    </style>

    <style name="SubtitleTextStyle" parent="customTextStyle">
        <item name="android:textSize">40sp</item>
    </style>

    <style name="CounterTextStyle" parent="customTextStyle">
        <item name="android:textSize">30sp</item>
    </style>

</resources>
